# 🏗️ SAHABAT3T DONOR SYSTEM - ARCHITECTURE DIAGRAM

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         🌐 PUBLIC INTERNET (Donors)                         │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
         ┌──────────▼──────────┐     │     ┌──────────▼──────────┐
         │  FRONTEND (Next.js) │     │     │  ADMIN/SCHOOL       │
         │                    │     │     │  (NOT DONOR)        │
         │ /app/donor/        │     │     │  /app/admin/        │
         │ /app/school/       │     │     │  /app/school/       │
         │                    │     │     │                    │
         └──────────┬─────────┘     │     └────────────────────┘
                    │               │
         ┌──────────▼──────────────┐│┌─────────────────────────┐
         │   API CALLS             ││ (Admin/School routes)    │
         │ lib/donor-api.ts        ││ (Requires Auth)          │
         └──────────┬──────────────┘└────────┬────────────────┘
                    │                       │
        ┌───────────┴──────────────────────┴─────────────────┐
        │                                                    │
        │           🔒 API GATEWAY (Express)                │
        │                                                    │
        │  /api/donor/*          /api/admin/*   /api/school│
        │  (No Auth Required)    (Auth Required)(Auth Req)  │
        │  (Public & Safe)      (Private)      (Private)   │
        │                                                    │
        └─────────────────────────┬──────────────────────────┘
                                  │
        ┌─────────────────────────┼──────────────────────────┐
        │                         │                         │
        ▼                         ▼                         ▼
  ┌──────────────┐         ┌────────────────┐      ┌──────────────┐
  │ Donor Routes │         │ Admin Routes   │      │ School Routes│
  │              │         │                │      │              │
  │ (9 GET/POST) │         │ (Campaign Mgmt)│      │ (Profile Mgmt│
  │              │         │ (Verification) │      │ (Reports)    │
  │ - Campaigns  │         │ (Reports)      │      │              │
  │ - Reports    │         │                │      │              │
  │ - Donations  │         │ UNCHANGED ✅   │      │ UNCHANGED ✅ │
  │              │         │                │      │              │
  └──────┬───────┘         └────────────────┘      └──────────────┘
         │
         │ donor.controller.js
         │ - getAllCampaigns()
         │ - getCampaignDetail()
         │ - searchCampaigns()
         │ - getCampaignTransparencyReport()
         │ - getPlatformStats()
         │ - getVerifiedSchools()
         │ - submitDonationSimulation()
         │ - getCampaignDonations()
         │ - getTopDonors()
         │
         └──────────────┬──────────────────┐
                        │                  │
            ┌───────────▼─────┐  ┌────────▼──────────┐
            │   MongoDB       │  │  Data Models     │
            │                │  │                  │
            │ Collections:   │  │ ✅ Campaign      │
            │ - Campaigns    │  │ ✅ School        │
            │ - Schools      │  │ ✅ Donation      │
            │ - Donations    │  │ ✅ Users         │
            │ - Users        │  │ ✅ Timeline      │
            │ - Timeline     │  │                  │
            │ - Documents    │  │ (Admin/School)   │
            │                │  │ ✅ Report        │
            └────────────────┘  │ ✅ Verification  │
                                │                  │
                                └──────────────────┘
```

---

## Component Architecture (Frontend)

```
┌─────────────────────────────────────────────────────────────────┐
│                   /app/donor/layout.tsx                          │
│           (Header + Main Content + Footer)                       │
└──────┬────────────────────────────────────────────────┬──────────┘
       │                                                │
       ▼                                                ▼
┌─────────────────────┐                    ┌─────────────────────┐
│  DonorHeader        │                    │  DonorFooter        │
│                     │                    │                     │
│ - Logo & Brand      │                    │ - About             │
│ - Navigation        │                    │ - Links             │
│ - Mobile Menu       │                    │ - Contact           │
│ - Responsive        │                    │ - Social Media      │
└─────────────────────┘                    └─────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│              Main Content Area (Routes)                           │
├──────────────┬────────────────────┬──────────────────┬───────────┤
│              │                    │                  │           │
│ /donor       │ /donor/campaigns   │ /donor/campaigns │  Other    │
│              │                    │ /[id]            │           │
│ Landing Page │ Campaign List      │ Campaign Detail  │           │
│              │                    │                  │           │
├──────────────┼────────────────────┼──────────────────┼───────────┤
│              │                    │                  │           │
│ • Hero       │ • Filters (Grid)   │ • Header         │           │
│ • Stats      │ • Search Box       │ • Progress Bar   │           │
│ • Trust      │ • Campaign Cards   │ • Description    │           │
│ • Featured   │ • Sort Options     │ • Timeline       │           │
│ • CTA        │ • Pagination (?)   │ • Fund Alloc     │           │
│ • Footer     │ • Empty State      │ • Form           │           │
│              │ • Footer           │ • Trust Info     │           │
│              │                    │ • Footer         │           │
└──────────────┴────────────────────┴──────────────────┴───────────┘
```

---

## Component Hierarchy

```
App
├── DonorLayout
│   ├── Header
│   │   ├── Logo
│   │   ├── Navigation (Desktop)
│   │   └── MobileMenu
│   │
│   ├── Main Content (Dynamic based on route)
│   │   ├── LandingPage
│   │   │   ├── HeroSection
│   │   ├── StatsSection
│   │   │   ├── Stat Box (×4)
│   │   │   │
│   │   │   ├── TrustSection
│   │   │   │   └── TrustCard (×4)
│   │   │   │
│   │   │   ├── FeaturedCampaigns
│   │   │   │   └── CampaignCard (×4)
│   │   │   │       ├── VerificationBadge
│   │   │   │       ├── ProgressBar
│   │   │   │       └── CTA Button
│   │   │   │
│   │   │   └── CTASection
│   │   │
│   │   ├── CampaignListPage
│   │   │   ├── Sidebar
│   │   │   │   ├── SearchBox
│   │   │   │   ├── CategoryFilter
│   │   │   │   └── SortDropdown
│   │   │   │
│   │   │   └── Grid
│   │   │       └── CampaignCard (×8+)
│   │   │           ├── Image
│   │   │           ├── VerificationBadge
│   │   │           ├── Title
│   │   │           ├── Description
│   │   │           ├── ProgressBar
│   │   │           └── CTAButton
│   │   │
│   │   └── CampaignDetailPage
│   │       ├── Header (Title, Badge, Category)
│   │       ├── ImagePlaceholder
│   │       ├── ProgressStats
│   │       │   ├── ProgressBar (Large)
│   │       │   └── Stats (×3)
│   │       ├── Description
│   │       ├── FundAllocation
│   │       │   └── AllocationBar (×4)
│   │       ├── Timeline
│   │       │   └── TimelineEvent (×5)
│   │       ├── SchoolInfo
│   │       │
│   │       └── Sidebar
│   │           ├── DonationForm
│   │           │   ├── NameInput
│   │           │   ├── PredefinedAmounts
│   │           │   ├── CustomAmountInput
│   │           │   └── SubmitButton
│   │           └── TrustIndicators (×3)
│   │
│   └── Footer
│       ├── About
│       ├── Links
│       ├── Contact
│       └── Social Media
```

---

## Data Flow Diagram

```
┌─────────────┐
│   Donor     │
│   (User)    │
└──────┬──────┘
       │
       │ 1. Browse to /donor
       ▼
┌─────────────────────────────────┐
│  Landing Page                   │
│  - Shows featured campaigns     │
│  - Shows platform stats         │
│  - Shows trust info             │
└──────┬──────────────────────────┘
       │
       │ 2. Click "Explore Campaigns"
       ▼
┌─────────────────────────────────┐
│  Campaign List                  │
│  - Fetch /api/donor/campaigns   │
│  - Display all campaigns        │
│  - Allow search/filter/sort     │
└──────┬──────────────────────────┘
       │
       │ 3. Click campaign card
       ▼
┌─────────────────────────────────┐
│  Campaign Detail                │
│  - Fetch /api/donor/campaigns/:id
│  - Show full details            │
│  - Show timeline                │
│  - Show fund allocation         │
│  - Show donation form           │
└──────┬──────────────────────────┘
       │
       │ 4. Fill donation form
       │    - Enter name
       │    - Enter amount
       ▼
       │ 5. Click submit
       ▼
┌─────────────────────────────────┐
│  Submit Donation Simulation     │
│  POST /api/donor/donations/     │
│  simulate                       │
│  {                              │
│    campaignId,                  │
│    donorName,                   │
│    amount,                      │
│    isSimulation: true           │
│  }                              │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Success Message                │
│  - Show confirmation            │
│  - Thank donor                  │
│  - Offer next steps             │
└─────────────────────────────────┘
```

---

## API Request/Response Flow

```
FRONTEND REQUEST:
┌────────────────────────────────────┐
│  getDonorCampaigns()               │
│  (from lib/donor-api.ts)           │
└────────┬─────────────────────────┬─┘
         │ axios.get()             │
         │                         │
         ▼                         ▼
    ┌─────────────┐          ┌──────────────┐
    │ HTTP GET    │          │ URL:         │
    │             │          │ /api/donor/  │
    │ No Auth     │          │ campaigns    │
    │ No Body     │          │              │
    └─────────────┘          └──────────────┘

BACKEND PROCESSING:
┌──────────────────────────────────────┐
│  Express Route Middleware            │
│  /api/donor/campaigns (GET)          │
└────────────┬───────────────────────┬─┘
             │                       │
             ▼                       ▼
       ┌──────────────────────────────────┐
       │ donorController.getAllCampaigns()│
       │                                  │
       │ 1. Query DB                      │
       │    Campaign.find({status: ...})  │
       │                                  │
       │ 2. Exclude admin fields          │
       │    .select('-adminNotes ...')    │
       │                                  │
       │ 3. Format response               │
       │    Map campaign object           │
       │                                  │
       │ 4. Return JSON                   │
       └──────────────────┬───────────────┘
                          │
BACKEND RESPONSE:
                          ▼
                ┌─────────────────────┐
                │ HTTP 200            │
                │ Content-Type: JSON  │
                │                     │
                │ {                   │
                │   success: true,    │
                │   data: [           │
                │     {               │
                │       id: "...",     │
                │       title: "...",  │
                │       verified: ..., │
                │       ...            │
                │     }               │
                │   ]                 │
                │ }                   │
                └──────────┬──────────┘
                           │ Response travels back to frontend
                           ▼
FRONTEND PROCESSING:
                ┌─────────────────────┐
                │ .then()             │
                │ res.data returned    │
                │ State updated       │
                │ Component re-render  │
                └─────────────────────┘
```

---

## Role-Based Access Control (RBAC)

```
┌──────────────────────────────────────────────────────────────┐
│                    REQUEST COMES IN                           │
└────────┬─────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────┐
│  Which route is requested?                                   │
└────────┬─────────────┬──────────────┬────────────────────────┘
         │             │              │
    ┌────▼────┐   ┌────▼────┐   ┌────▼────┐
    │ /donor   │   │ /admin  │   │ /school │
    │          │   │         │   │         │
    │ ✅ Allow │   │ ✅ Check│   │ ✅ Check│
    │ Access   │   │ Auth    │   │ Auth    │
    │          │   │         │   │         │
    └────┬─────┘   └────┬────┘   └────┬────┘
         │              │             │
         ▼              ▼             ▼
    ┌─────────┐  ┌──────────┐  ┌──────────┐
    │ Public  │  │ Admin    │  │ School   │
    │ Routes  │  │ Auth OK? │  │ Auth OK? │
    │         │  │          │  │          │
    │ No Auth │  │ Yes ✅   │  │ Yes ✅   │
    │ Needed  │  │ No ❌    │  │ No ❌    │
    │         │  │ Reject   │  │ Reject   │
    └────┬────┘  └────┬─────┘  └────┬─────┘
         │            │            │
         ▼            ▼            ▼
    ┌─────────┐  ┌──────────┐  ┌──────────┐
    │ Return  │  │ Return   │  │ Return   │
    │ Public  │  │ Admin    │  │ School   │
    │ Data    │  │ Data     │  │ Data     │
    │         │  │ (Full)   │  │ (Full)   │
    │ ✅ Safe │  │ ✅ Safe  │  │ ✅ Safe  │
    └─────────┘  └──────────┘  └──────────┘
```

---

## Deployment Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                      CLIENT BROWSER                            │
│                    (Any location)                              │
└────────────────────────────────────────────────────────────────┘
                              ▲
                              │ HTTPS
                              │
                ┌─────────────┴──────────────┐
                │                            │
         ┌──────▼──────┐             ┌──────▼──────┐
         │   Frontend   │             │  CDN        │
         │   (Next.js)  │             │  (Optional) │
         │              │             │             │
         │ Vercel/etc   │             │ CloudFlare  │
         │              │             │ AWS         │
         │ ✅ Served    │             │             │
         │    at        │             │ ✅ Caches   │
         │    domain.com│             │    assets   │
         └──────┬───────┘             └─────────────┘
                │
                │ API Calls to
                │
         ┌──────▼──────────────────┐
         │   Backend API           │
         │   (Node.js + Express)   │
         │                         │
         │   Heroku/AWS/GCP        │
         │                         │
         │ /api/donor/*   ✅       │
         │ /api/admin/*   ✅       │
         │ /api/school/*  ✅       │
         └──────┬──────────────────┘
                │
                │ Database queries
                │
         ┌──────▼──────────────────┐
         │   MongoDB (Atlas)       │
         │                         │
         │ - Campaigns            │
         │ - Schools              │
         │ - Donations            │
         │ - Users                │
         │ - Timeline             │
         │ - Documents            │
         │                         │
         │ ✅ Hosted in cloud     │
         └─────────────────────────┘
```

---

## File Organization

```
project/
│
├── app/                           # Frontend Pages (Next.js)
│   ├── donor/
│   │   ├── page.tsx              # Landing page
│   │   ├── layout.tsx            # Donor layout wrapper
│   │   └── campaigns/
│   │       ├── page.tsx          # Campaign list
│   │       └── [id]/
│   │           └── page.tsx      # Campaign detail
│   │
│   ├── admin/                    # Admin pages (existing, NOT MODIFIED)
│   ├── school/                   # School pages (existing, NOT MODIFIED)
│   ├── layout.tsx                # Root layout
│   ├── page.tsx                  # Root home
│   └── globals.css               # Global styles with theme
│
├── components/
│   ├── donor/                    # Donor-specific components
│   │   ├── Header.tsx            # Navigation + Footer
│   │   ├── CampaignCard.tsx      # Campaign preview
│   │   ├── ProgressBar.tsx       # Progress visualization
│   │   ├── VerificationBadge.tsx # Verified indicator
│   │   ├── DonationForm.tsx      # Donation form
│   │   ├── TimelineComponent.tsx # Timeline display
│   │   └── TrustSection.tsx      # Trust section
│   │
│   └── ui/                       # Shared UI (existing)
│       ├── Button.tsx
│       ├── Input.tsx
│       └── Card.tsx
│
├── lib/
│   ├── donor-api.ts              # Donor API utilities
│   ├── api.ts                    # General API (existing)
│   ├── axios.ts                  # Axios setup (existing)
│   └── utils.ts                  # Utilities (existing)
│
├── backend/
│   ├── routes/
│   │   ├── donor.routes.js       # Donor routes (NEW)
│   │   ├── admin.routes.js       # Admin (existing, NOT MODIFIED)
│   │   └── school.routes.js      # School (existing, NOT MODIFIED)
│   │
│   ├── controllers/
│   │   ├── donor.controller.js   # Donor logic (NEW)
│   │   ├── adminController.js    # Admin (existing)
│   │   └── schoolController.js   # School (existing)
│   │
│   ├── models/
│   │   ├── Campaign.js           # (existing)
│   │   ├── School.js             # (existing)
│   │   ├── Donation.js           # (new, for donations)
│   │   ├── User.js               # (existing)
│   │   └── Timeline.js           # (existing)
│   │
│   ├── middleware/
│   │   ├── auth.js               # (existing)
│   │   ├── adminAuth.js          # (existing)
│   │   └── schoolAuth.js         # (existing)
│   │
│   ├── server.js                 # Main server (UPDATED to add donor routes)
│   └── config/
│       └── database.js           # (existing)
│
├── Documentation/
│   ├── DONOR_SYSTEM_GUIDE.md
│   ├── SERVER_SETUP_GUIDE.js
│   ├── IMPLEMENTATION_CHECKLIST.md
│   ├── PROJECT_COMPLETION_SUMMARY.md
│   └── QUICK_START.md
│
└── Configuration
    ├── package.json
    ├── tsconfig.json
    ├── tailwind.config.ts or (uses globals.css)
    ├── postcss.config.mjs
    ├── next.config.ts
    └── .env.local
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                     BROWSER (Client)                        │
│  - No admin links visible                                  │
│  - No admin forms visible                                  │
│  - Read-only components only                               │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTPS Only
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   NETWORK LAYER                              │
│  - CORS configured                                          │
│  - HTTPS enforced                                           │
│  - Request validation                                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   API GATEWAY                                │
│                                                             │
│  /api/donor/*      → No Auth, Public                       │
│  /api/admin/*      → Admin Auth Required                   │
│  /api/school/*     → School Auth Required                  │
└────────────────────────┬────────────────────────────────────┘
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
┌──────────────┐ ┌─────────────┐ ┌──────────────┐
│ Donor Routes │ │Admin Routes │ │School Routes │
│ (Public)     │ │ (Private)   │ │ (Private)    │
└──────┬───────┘ └──────┬──────┘ └──────┬───────┘
       │                │               │
       └────────────────┼───────────────┘
                        ▼
         ┌──────────────────────────────┐
         │   APPLICATION LAYER          │
         │   - Input validation         │
         │   - Business logic           │
         │   - Data transformation      │
         │   - Admin fields excluded    │
         └──────────────┬───────────────┘
                        │
                        ▼
         ┌──────────────────────────────┐
         │   DATABASE LAYER             │
         │   - MongoDB                  │
         │   - Data encryption          │
         │   - Access control           │
         └──────────────────────────────┘
```

---

**Architecture Diagram Complete! 🏗️**

For implementation details, see:
- `DONOR_SYSTEM_GUIDE.md` - Full documentation
- `SERVER_SETUP_GUIDE.js` - Integration instructions
- `IMPLEMENTATION_CHECKLIST.md` - Deployment checklist
